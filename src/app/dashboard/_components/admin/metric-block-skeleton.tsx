export const MetricBlockSkeleton = () => {
  return (
    <div className="skeleton-loading flex flex-1 flex-col gap-2.5 p-4">
      {/* <h2 className="font-bold uppercase">
        <span className="sr-only">{header}</span>
        <span className="inline-block wrap-break-word" aria-hidden="true">
          <span className="mr-2">&gt;</span>
          {`${header.replace(/\s+/g, '_')}`}
        </span>
      </h2>

      <p className="text-accent font-bold">{value}</p> */}
    </div>
  )
}
